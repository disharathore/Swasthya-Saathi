name: swasthya_saathi_final
description: "A new Flutter project."

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1
dependencies:
  flutter:
    sdk: flutter
  barcode_scan2: ^4.4.0  # Add this line
  archive: ^3.5.1
  shared_preferences: ^2.2.3
  firebase_core: ^3.1.1 # Add this
  cloud_firestore: ^5.0.2 # Add this
  firebase_auth: ^5.1.1 # Add this
  cupertino_icons: ^1.0.8
  qr_flutter: ^4.1.0 # Add this

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true